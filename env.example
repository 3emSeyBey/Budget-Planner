# Database Configuration
# For PlanetScale (recommended for Vercel)
DATABASE_URL=mysql://username:password@host:port/database_name?ssl={"rejectUnauthorized":true}

# Alternative: Individual database parameters
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=budget_planner
DB_PORT=3306

# JWT Secret for authentication (if needed)
JWT_SECRET=your_jwt_secret_key_here

# Vercel Environment
VERCEL_ENV=development
